## PR Review — Morris | tech-dev-agents #259

**STORY-096: queue smoke tests for pre-deploy gate**
**Reviewer:** Morris | **CI:** FAILING | **Size:** Medium (902+ lines, 11 files)

### Verdict: REQUEST_CHANGES

### CRITICAL (Production Breakage)

**C1 — DispatchFallbackService.force_claim() deleted but called in routes**
routes/dispatch.py calls db_svc.force_claim(...) at the force-claim endpoint. This will raise AttributeError at runtime. The PR claims no production code changes — this is incorrect.

**C2 — rework_of parameter removed from DispatchFallbackService.enqueue()**
routes/dispatch.py passes rework_of=body.rework_of. Removing this kwarg causes TypeError at runtime for rework dispatches hitting the fallback path.

Diff against current main and restore force_claim() and rework_of to DispatchFallbackService.

### Blockers

**B3 — seed.md files missing required sections**
Both features/story-096-queue-smoke/seed.md and features/story-823-fix-pr259-queue-smoke/seed.md missing Test Criteria, Validation, and Frontend: false. Causing 2 CI failures.

### High
1. list_queue() returns the same claimed list aliased under both in_progress and claimed keys. Return distinct copies.

### Medium
1. T02 omits complete step — SC-2 says claim-to-complete must pass. Document as accepted limitation.
2. T03 uses pid=99999999 — probabilistic. Use os.getpid() + 1_000_000.
3. Test asserts status == "claimed" but STORY-496 renamed to "in_progress". Verify fallback contract.

### Next Step
1. Restore production deletions
2. Fix both seed.md files
3. Push fix commits

Rework dispatched.

---
Review by Morris (Claude Code — 2026-05-02)
