## PR Review — Morris | tech-dev-agents #261

**STORY-795: Rework fleet vigilance post-merge sweep**
**Reviewer:** Morris | **CI:** FAILING | **Size:** Small (815+ lines, 9 files)

### Verdict: REQUEST_CHANGES

### Blockers

**B1 — seed.md missing required sections (CI gating failure)**
features/story-795/seed.md is missing Test Criteria and Validation sections (required post 2026-04-22) and Frontend: false field (required post 2026-04-26). Causing 3 failing assertions in test_sdlc_framework_compliance.py.

Fix: Add ## Test Criteria, ## Validation, and | Frontend | false | to the seed.

**B2 — _validate_api_key() defined but never called in check_post_merge_sweep()**
The API key guard (S-1 acceptance criterion) is a dead letter. The sweep runs without any API key check. Call _validate_api_key() at the entry of check_post_merge_sweep(), or remove it and the test.

### High

**H1 — Git log parser fragile: file paths with pipe break header detection**
The current heuristic misclassifies file paths containing pipe characters. Use re.match on the 40-char SHA hex pattern instead.

**H2 — DEFAULT_LOOKBACK_HOURS = 24 + SHA range creates a silent gap**
Merges between last_sha and 24h-ago are skipped after outages. Remove --since or widen to 7 days.

### Medium
1. DEPLOY_RELEVANT_PREFIXES includes bare scripts/ — matches any scripts dir. Document or anchor to deployment/scripts/.
2. WARN/CRIT threshold based on commit count, not significance. A single migration is more impactful than 3 config nits.
3. _run_post_merge_sweep() adds post_merge_sweep as top-level key (not inside blind_spot_checks). Confirm consumer handles both shapes.

### Next Step
Fix B1 + B2, then push. Rework dispatched.

---
Review by Morris (Claude Code — 2026-05-02)
