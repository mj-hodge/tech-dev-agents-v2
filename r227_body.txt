## PR Re-Review --- Morris | tech-dev-agents #227 --- STORY-738 (Round 4)

**Commit reviewed:** `7f9e7f1` ("fix(STORY-738): cap answer textarea at 65536 chars")
**CI:** PASS (6/6 checks green) | **Verdict:** REQUEST_CHANGES --- H-2 still not fixed

### Fix Verification
| Item | Status | Evidence |
|------|--------|----------|
| Mark's textarea cap (`maxLength={65536}`) | **VERIFIED** | Frontend cap matches `AnswerRequest.answer: Field(..., max_length=65536)` Pydantic model and route-level `len(question_text) > 65536` guard. Counter UI (`{answer.length} / 65,536`) included. End-to-end consistency. |
| H-2: gating-asymmetry documentation | **NOT FIXED** | feature-spec.md still goes 2.1 → 2.2 → 2.3 → 2.4 with no Section 2.3a. Sections 2.2/2.3 gate the GET/POST retrieval endpoints; Section 2.1 (needs_info() write path) has no mention of gating. The asymmetry is intentional but undocumented. |

### What Is Still Missing
A future reader cannot tell from the spec why the write path is ungated while the retrieval paths are gated. They would have to re-derive it from the route code. The design choice should be stated explicitly.

### Required Action --- Exact Text to Add
Add to `features/story-738-needs-info-response-ui/feature-spec.md` between Section 2.3 and Section 2.4:

```
### 2.3a Gating Strategy --- Intentional Asymmetry

**Design decision:** The needs_info() service method (Section 2.1) is NOT gated by
OPS_DISPATCH_NEEDS_INFO_ENABLED. The write path is always active. Only the retrieval
paths (Sections 2.2 and 2.3) are gated.

**Rationale:** Agents invoke POST /dispatch/needs-info/{story_id} to transition state ---
an existing ungated operation. question_text is an additive extension. The NEW
capabilities requiring a feature flag are operator UI features: reading questions (GET)
and posting answers (POST). Agents are not blocked when the feature is disabled.

**Operational implication:** With gate OFF --- question_text is written to DB but cannot
be retrieved. With gate ON --- full question/answer flow available. This is intentional.
```

Documentation-only change. Once `2.3a` is added, this is an APPROVE --- backend 22/22 GREEN, frontend modal logic sound, textarea cap correct end-to-end.

*Review by Morris (Claude Code deep analysis) 2026-05-01 — Round 4*
