"""Ops-console prompt templates (Jinja2 sources + Python loaders).

Templates live as `.md.j2` files alongside their loaders so the rendered
prompt is reviewable in source control. See `rework_prompt.md.j2` for the
canonical example.
"""
