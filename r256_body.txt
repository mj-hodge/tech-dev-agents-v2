## PR Review --- Morris | tech-dev-agents #256 --- STORY-802: Daily Cost Alert + VM Config Audit

**Size:** Large (1155 lines, 15 files) | **CI:** FAIL (Python contract + unit tests) | **Verdict:** REQUEST_CHANGES

### SDLC Compliance: PASS
All deliverables present: seed.md, analysis.md, feature-spec.md, test-design.md, verification.md, audit.md.

### Findings

- **BLOCKER**: CI failing --- Python contract + unit tests. Root cause: OPUS_PHASES removes Phase 6 from Opus, but test_phase_6_includes_model_opus was not updated. Fix: rename to test_phase_6_excludes_model_opus and flip assertion. (tests/deployment/test_sdk_model_cli_contract.py:198)
- **HIGH**: AlertService.process_cost_alerts() calls await self._loki.query_cost_alerts(None, start, end) but no evidence this method exists on LokiClient in production. Will raise AttributeError at runtime. Verify it is implemented or add it. (tech_dev_agents/ops_console/services/alert_service.py:723)
- **HIGH**: daily_cost_alert.sh --- SQL WHERE clause uses string-interpolated TODAY without psql -v binding. Prefer psql -v today=TODAY pattern for resilience. (deployment/ops-console/scripts/daily_cost_alert.sh:77)
- **MEDIUM**: .sdlc submodule updated but PR body does not describe what changed. Add one line.
- **NIT**: feature-spec references 2026-05-08 cost gate follow-up with no linked ticket. Ensure it is queued before merge.

### Required Before Merge
1. Fix test: update test_sdk_model_cli_contract.py:198 --- rename and flip assertion to confirm Phase 6 EXCLUDES Opus
2. Confirm LokiClient.query_cost_alerts() exists in production code (not just mocked)
3. Document the .sdlc submodule change in PR body

Dispatching fix story now.

*Review by Morris (Claude Code deep analysis) 2026-05-01*