## Adversarial Review — Morris | advertising-amazon #299 — STORY-642 (manual adversarial pass)

Note: This PR was approved by Morris on initial review. This is a supplemental adversarial pass performed 2026-05-01 to satisfy the dual-review gate.

**Adversarial Verdict:** APPROVE — no blockers found. LOW findings documented below.

### Migration 045_competitor_self_flag.py — Adversarial Assessment

- **REVERSIBILITY (PASS)**: `downgrade()` is fully implemented — drops `v_current_competitor_metrics` view then drops `is_self` column via `op.drop_column`. Reversible.
- **NULL HANDLING (PASS)**: Column defined as `nullable=False, server_default="false"`. Existing rows default to `FALSE` at migration time. No NULL propagation risk.
- **LOW — View Recreation Race**: `upgrade()` drops and recreates `v_current_competitor_metrics`. Any active queries against that view between `DROP VIEW` and `CREATE VIEW` will fail with "view does not exist." In practice, Alembic runs this in a transaction, so the window only exists between the transaction commit and query execution. Document this in the migration header for ops awareness.
- **LOW — Table Guard Logic**: The `IF NOT EXISTS` guard (checking `information_schema.tables`) means migration 045 is a no-op if `competitor_metrics_snapshots` does not exist yet. This is intentional (STORY-641 pending). However, after STORY-641 ships, there is no mechanism to retroactively apply the guard. Confirm that STORY-641's create-table migration explicitly includes `is_self BOOLEAN NOT NULL DEFAULT FALSE` so the column is present from inception, making a second run of 045 also a no-op rather than a double-add error.

### BSR Alert Service — Loki Down Scenario

- **LOW — Loki outage**: If Loki is unavailable, structured log emission (`emit_bsr_alert`) will fail or block. The alert service should treat Loki as fire-and-forget (non-blocking). If structlog is configured with a `QueueHandler` or async sink, this is already handled. If not, a Loki outage could stall the alert processing loop. Confirm the Loki sink has a timeout and failure mode that does not block the async event loop.
- **NIT**: BSR_ALERTS_ENABLED feature flag added to `.env.example` — correct approach for safe rollout.

### Credentials Handling

- No hardcoded credentials in any file in the diff.
- SP-API credentials are not referenced at runtime in the alert service (Approach A reads only from DB — zero outbound HTTP by design, as confirmed by `respx --strict` in test suite).
- `.env.example` entry (`BSR_ALERTS_ENABLED=false`) is a boolean flag, not a credential. Safe to commit.

### Summary
Migration is sound, reversible, and NULL-safe. The two LOW items (view recreation window, STORY-641 column coordination) are documented for operator awareness. No security blockers. No adversarial blockers.

**Dual-review gate status:** Claude Code (Morris, initial review) + Manual adversarial pass (Morris, 2026-05-01). APPROVE confirmed.

*Review by Morris (supplemental adversarial pass) 2026-05-01*
